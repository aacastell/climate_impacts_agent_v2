# Floor, not a pin — see repo README convention on not hard-coding figures
# that go stale invisibly. `pip install -r requirements.txt` resolves to the
# current release.
httpx>=0.27.0
boto3>=1.34.0
dvc[s3]>=3.50.0
xarray>=2024.3.0
netCDF4>=1.6.5
numpy>=1.26.0
# open_mfdataset (used to concatenate each variable's decadal files) requires dask to combine
# them lazily — a real production dependency for the process stage, not just for benchmarking.
dask>=2024.5.0
